# Keep the /api suffix. Do not append /user/api/v1 here.
SIMHOST_API_BASE_URL=https://app.simhost.io/api
# Paste the full token only, without the word Bearer.
SIMHOST_API_TOKEN=PASTE_YOUR_FULL_TOKEN_HERE
